---
stages:
  - validate
  - format

default:
  image:
    name: hashicorp/terraform:1.7.2
    entrypoint: [""]
  before_script:
    - terraform init -backend=false

validate:
  stage: validate
  script:
    - terraform validate

format:
  stage: format
  script:
    - terraform fmt -check -recursive -diff
